# Write A Superpowers Plan

Load `.agents/skills/writing-plans/SKILL.md` and follow it.

Use this workflow after a design, spec, or clear requirements exist. Save the plan to `docs/superpowers/plans/YYYY-MM-DD-<feature>.md`, include exact file paths and verification commands, and make the tasks small enough to execute independently.
