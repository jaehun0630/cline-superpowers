# Execute A Superpowers Plan

If Cline has subagent support, load `.agents/skills/subagent-driven-development/SKILL.md` and follow it. Otherwise load `.agents/skills/executing-plans/SKILL.md`.

Use this workflow when a written implementation plan already exists. Read the plan, verify it is coherent, execute task by task, run the requested checks, and report blockers with the exact failing evidence.
