# Debug With Superpowers

Load `.agents/skills/systematic-debugging/SKILL.md` and follow it before proposing fixes.

Use this workflow for bugs, failing tests, unexpected behavior, flaky behavior, or production incidents. Find the root cause first, gather evidence, apply the smallest justified fix, and verify the fix with a fresh command.
