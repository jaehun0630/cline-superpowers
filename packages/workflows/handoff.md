# Handoff

Load `.agents/skills/handoff/SKILL.md` and follow it.

Use this workflow when the user wants to summarize the current conversation for a fresh agent, continue work in another session, or prepare a handoff document. Save the handoff outside the repository in the OS temporary directory, reference existing artifacts instead of duplicating them, and redact sensitive information.
